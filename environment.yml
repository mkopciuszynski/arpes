name: arpes
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.10

  - astropy
  - xarray>=2023.5.0
  - h5py>=3.2.1
  - pyqtgraph>=0.12.0,<0.14.0

  - pint
  - pandas
  - numpy>=1.22.0,<2.0.0
  - scipy>=1.7.0,<2.0.0
  - lmfit>=1.1.0,<2.0.0
  - netCDF4>=1.5.0,<2.0.0

  # plotting
  - colorcet
  - matplotlib>=3.5.0
  - bokeh>=3.0.0,<4.0.0
  - ipywidgets>=7.0.1,<8.0.0

  # Misc deps
  - scikit-learn>=1.0.0,<2.0.0
  - pip

  # pip
  - pip:
      - PyQt5==5.15
      - packaging
      - numba>=0.55.0,<1.0.0
      - colorama
      - imageio
      - titlecase
      - tqdm
      - rx
      - dill
      - ase>=3.17.0,<3.22.0
      - -e .
